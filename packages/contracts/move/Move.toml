# Neura Move package
# Verifier-compatible: no explicit Sui dep so both CLI and Welldone auto-add same framework
[package]
name = "neura"
version = "0.1.0"
edition = "2024.beta"

[addresses]
neura = "0x0"
